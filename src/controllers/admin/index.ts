import {categories} from './categories';
import {admin} from './admin';
import { users } from './users';
import settings from './settings';

const adminControllers = {
    categories, admin, users, settings
}

export default adminControllers;