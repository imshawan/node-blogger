<div class="post-item bg-light p-3">
    <% if (post.featuredImage) { %>
        <img src="<%= post.featuredImage %>" class="card-img" alt="...">
    <% } %>
    <div class="post-meta text-normal d-flex justify-content-between">
        <span><i class="far fa-user"></i> Posted by <%= post?.author.fullname || post?.author.username %></span><span><i class="far fa-calendar"></i>
            <%= post.createdAt %>
        </span>
    </div>
    <a href="/posts/<%= post.slug %>" class="post-title text-normal"><%= post.title %></a>
    <p class="post-content text-ellipse-4"><%= post.blurb %></p>
    <div class="post-meta d-flex justify-content-between">
        <div>
            <% if (_application.authenticated) { %>
                <span>
                    <i data-post-id="<%= post.postId %>" data-post-action="<% if (post.hasLiked) { %>unlike<% } else { %>like<% } %>" 
                        class="fa <% if (post.hasLiked) { %> fa-thumbs-up <% } else { %> fa-thumbs-o-up <% } %> cursor-pointer me-2"></i>
                    <span><%= post.likes %></span>
                </span>
            <% } %>
            <span><i class="fa fa-comment-alt me-2"></i>
                <span><%= post.comments %></span>
            </span>
            <span><i class="fa fa-eye me-2"></i>
                <span><%= post.views %></span>
            </span>
        </div>
        <div>
            <a href="/posts/<%= post.slug %>" class="read-more">Read more</a>
        </div>
    </div>
</div>